Copy your 5 project images into this folder with these names:

  render-01.png  <- first project image (render_final_001)
  render-02.png  <- second project image (render_final_0011)
  render-03.png  <- third project image (render_final_002)
  render-03-raw.png  <- raw Blender render (source for third image)
  render-03-canny.png <- Canny edge map used as Flux input

Then the Brainwave Architecture project page will display them correctly.
